<table class="table table-striped table-hover ">

<thead>
<tr>
  <th>Student</th>
  <th>Assignment</th>
  <th>Description</th>
  <th>Due Date</th>

</tr>
</thead>
<tbody>
  <%console.log(staffHome);%>
<% for (let i = 0; i < staffHome.length; i++){%>
  <% let {student_id, id, date_due, staff_id, staff_first, staff_last, student_first, student_last, assignment_name, description, completed } = staffHome[i] %>
<tr>
  <td><%= student_first %> <%= student_last%></td>
  <td><%= todo[0].name %></td>
  <td><%= todo[0].description %></td>
  <form action="/staff/assign" method="post">
  <td><input type="date" name="date_due"><td>
  <td>
  <input type="hidden" name="student_id" value="<%=student_id%>">
  <input type="hidden" name="todo_id" value="<%=todo[0].id%>">
  <button type="submit">Assign to Student</button></td>
  </form>
</tr>
<%}%>
</tbody>
</table>
